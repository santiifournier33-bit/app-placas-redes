import Zernio from '@zernio/node'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); async function run() { const apiKey = process.env.ZERNIO_API_KEY_1; const client = new Zernio({ apiKey }); try { const postRes = await client.posts.createPost({ body: { publishNow: true, content: '', profileId: 'dummy123', mediaItems: [], platforms: [ { platform: 'instagram', accountId: 'dummy456' } ] } }); } catch (err) { if (err.message && err.message.includes('JSON')) { console.log('Error message mentions JSON!'); } console.log(err.name); console.log(err.message); } } run();
