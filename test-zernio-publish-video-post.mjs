import Zernio from '@zernio/node'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); async function run() { const apiKey = process.env.ZERNIO_API_KEY_1; const client = new Zernio({ apiKey }); try { const publicUrl = 'https://freirepropiedades.com/test.mp4'; const body = { publishNow: true, content: 'Test post video', profileId: 'dummy123', mediaItems: [ { type: 'video', url: publicUrl } ], platforms: [ { platform: 'instagram', accountId: 'dummy456' } ] }; console.log('Post Body:', JSON.stringify(body, null, 2)); const postRes = await client.posts.createPost({ body }); console.log('? Zernio createPost response:', JSON.stringify(postRes.data, null, 2)); } catch (err) { console.error('Error in test:', err.message); if (err.details) console.error(err.details); } } run();
