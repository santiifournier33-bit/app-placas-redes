import Zernio from '@zernio/node'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); async function run() { const apiKey = process.env.ZERNIO_API_KEY_1; const client = new Zernio({ apiKey }); try { const presignRes = await client.media.getMediaPresignedUrl({ body: { filename: 'freire-placa.mp4', contentType: 'video/mp4', size: 1000000 } }); console.log('Presigned URL:', presignRes.data); } catch (err) { console.error('Error in presigned URL:', err); } } run();
