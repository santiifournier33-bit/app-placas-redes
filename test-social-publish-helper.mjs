import fetch from 'node-fetch'; async function run() { try { const res = await fetch('http://localhost:3000/api/social/publish', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'test@freire.com', 'text': 'test', 'mediaUrls': ['https://freirepropiedades.com/test.mp4'], 'socialAccountIds': ['dummy'], 'contentFormat': 'video' }) }); console.log('Status:', res.status); console.log('Status text:', res.statusText); const text = await res.text(); console.log('Raw response (start):', text.substring(0, 200)); try { const json = JSON.parse(text); console.log('Parsed JSON:', Object.keys(json)); } catch(e) { console.error('JSON Parse Error:', e.message); } } catch (err) { console.error('Fetch Exception:', err.message); } } run();
