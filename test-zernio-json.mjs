import Zernio from '@zernio/node'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); async function run() { const apiKey = process.env.ZERNIO_API_KEY_1; const client = new Zernio({ apiKey }); try { const postRes = await client.posts.createPost({ body: { publishNow: true, content: '', profileId: 'dummy123', mediaItems: [], platforms: [ { platform: 'instagram', accountId: 'dummy456' } ] } }); console.log('Post Res:', postRes.data); } catch (err) { console.error('Error JSON:', JSON.stringify(err, Object.getOwnPropertyNames(err), 2)); console.log('Error object keys:', Object.keys(err)); console.log('Is APIError?', err.constructor.name); } } run();
