import { getZernioKeyForUser } from './src/lib/zernio'; import Zernio from '@zernio/node'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); async function run() { const apiKey = getZernioKeyForUser('default@freire.com'); const client = new Zernio({ apiKey }); try { const list = await client.accounts.listAccounts(); console.log('Accounts:', list.data); } catch (err) { console.error(err); } } run();
